# Phase 3b: PCD-Sliced Grid + IMU Fix + Scan-Frame Fix — MCL vs NDT Ground Truth (map-quality isolation)

Statistics/thresholds below are auto-generated by `compare_poses.py`
(`--no-motion-window --gt-time-source bag`); re-run to refresh. This
section and Interpretation are hand-written and not touched by re-running
the tool.

- GT bag: `data/rosbags/phase3/sample_ndt_gt`
- PF bag: `data/rosbags/phase3/sample_pf_run_framefix_scanplane`
- Map: `occupancy_grid_scanplane.yaml` — the original PCD-sliced grid
- Fixes applied: same two as `2dlidar-phase3b-framefix-scanaccum.md` —
  `IMU_YAW_SIGN=-1.0` and `pointcloud_to_laserscan target_frame:=base_link`
  with `SCAN_MIN_HEIGHT=1.91611`/`SCAN_MAX_HEIGHT=2.21611` (sensor z above
  base_link = 2.06611 m, see that report for the derivation). Purpose:
  hold both fixes constant and vary only the map, isolating the residual
  map-quality effect now that both odometry and scan-frame bugs are
  corrected.

## Overall Result: **FAIL**

## Full-Overlap Statistics

| Metric | Value |
|---|---|
| n (pairs) | 814 |
| Trans. mean (m) | 32.2382 |
| Trans. RMS (m) | 54.7480 |
| Trans. max (m) | 160.7509 |
| Trans. p95 (m) | 133.5427 |
| Yaw mean \|err\| (rad) | 0.8787 |
| Yaw max \|err\| (rad) | 3.0958 |

## Motion-Window Statistics

_Motion-window analysis disabled (`--no-motion-window`); this bag moves throughout the recording, so a fixed stationary/motion split is not meaningful here. Only full-overlap statistics are reported._

## Thresholds (applied to full-overlap stats)

| Threshold | Value | Limit | Result |
|---|---|---|---|
| Mean translational error < 1.0 m | 32.2382 | 1.0000 | FAIL |
| p95 translational error < 2.5 m | 133.5427 | 2.5000 | FAIL |
| Mean \|yaw\| error < 0.2 rad | 0.8787 | 0.2000 | FAIL |

## Notes

- Replay rate: 1.0x for both GT and PF recordings, replayed with `--clock` (sim time).
- GT time source: `--gt-time-source=bag` (rosbag2 storage receive-time, used because the GT bag was itself replayed to produce the PF run; see _read_bag() docstring in compare_poses.py for why header.stamp would give zero overlap here).
- PF params: `range_method=cddt`, particle count per vendored `config/localize.yaml` default (4000), `max_range=30.0` (outdoor VLP-32C), `scan_topic=/scan`, `odometry_topic=/odom`.
- Odometry input to PF was synthesized wheel+IMU planar unicycle integration (`scripts/2dlidar/wheel_imu_odom.py`), not a native wheel encoder feed — odometry quality directly affects PF motion-model accuracy between scans.
- GT is Autoware NDT `/localization/kinematic_state`; PF publishes pose in the occupancy-grid frame, which is sliced from the same PCD map as NDT's map frame — poses are directly comparable without a transform.
- Z-band caveat: comparison is 2D (x, y, yaw) only; the GT track's z is Autoware's 3D NDT estimate while PF is inherently 2D, so z is not compared.
- `/scan` was bridged from `/sensing/lidar/velodyne_points` via `pointcloud_to_laserscan` with a z-band filter (see Task 3); QoS was bridged from `pointcloud_to_laserscan`'s best-effort publisher to `particle_filter`'s reliable-QoS subscription.
- **Diagnostic note (not a fix):** the run above FAILs the Phase 3 gate. Full-overlap trans. mean=32.24 m, p95=133.54 m, yaw mean|err|=0.879 rad (thresholds: mean<1.0 m, p95<2.5 m, yaw<0.2 rad). Plausible contributors: (1) the synthesized wheel+IMU odometry feeding PF's motion model has no absolute correction and can accumulate heading error, which a particle filter with too few effective particles or a poor sensor model may fail to correct via scan matching; (2) `range_method=cddt` / particle count / sensor-model noise parameters were carried over from vendored defaults and may not be tuned for this scenario; (3) possible scan-to-map mismatches (z-band filter choice, `max_range`) reducing effective localization likelihood signal; (4) ground-truth quality itself (see report notes on the GT source) may be a contributing factor. PF parameter tuning is explicitly out of scope for this gate and is deferred to a Phase-3 follow-up.

## Interpretation

**Scan-accumulated grid remains clearly superior once both bugs are
fixed.** With both the IMU sign fix and scan-frame fix held constant:

| Map | Trans. mean (m) | Trans. p95 (m) | Yaw mean\|err\| (rad) |
|---|---|---|---|
| PCD-sliced (scanplane) | 32.24 | 133.54 | 0.879 |
| Scan-accumulated | 18.96 | 106.30 | 0.575 |

The scan-accumulated grid beats the PCD-sliced grid on every metric,
by a wider margin than before either fix (raw-IMU: 31.0 vs 25.25 mean;
IMU-fix-only: 32.39 vs 20.16 mean). The scan-frame fix also helped the
PCD-sliced grid's yaw somewhat (yaw mean|err| 1.459 rad pre-frame-fix
[`2dlidar-phase3b-scanplane-imufix.md`] -> 0.879 rad here), so the
frame bug was real and map-independent, but translational error on this
map stayed essentially flat (32.39 -> 32.24 m) — the PCD-sliced grid's
structural mismatch with the 2D scan (the original scan-accumulation
hypothesis) is still the dominant unresolved issue for *this* map.
Combined picture across the full fix progression (scan-accumulated map,
raw odom -> IMU fix -> IMU+frame fix): mean 25.25 -> 20.16 -> 18.96 m;
p95 126.68 -> 89.81 -> 106.30 m; yaw 1.603 -> 1.318 -> 0.575 rad. Both
fixes were real and additive on yaw and (mostly) on mean error; p95
remains the outstanding gap, pointing at intermittent large-excursion
failures (not a smooth systematic bias) as the next thing to
characterize, alongside PF sensor-model/particle-count tuning.
