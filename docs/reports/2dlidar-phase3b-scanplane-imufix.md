# Phase 3b: PCD-Sliced Grid + IMU Yaw-Sign Fix — MCL vs NDT Ground Truth (map-quality isolation)

Statistics/thresholds below are auto-generated by `compare_poses.py`
(`--no-motion-window --gt-time-source bag`); re-run to refresh. This
section and the Interpretation section are hand-written and not touched
by re-running the tool.

- GT bag: `data/rosbags/phase3/sample_ndt_gt`
- PF bag: `data/rosbags/phase3/sample_pf_run_scanplane_imufix`
- Map: `occupancy_grid_scanplane.yaml` — the original PCD-sliced grid
  (same map as the `2dlidar-phase3b-sample-site.md` baseline, mean 31.0 m)
- Odometry fix: `IMU_YAW_SIGN=-1.0`, same as
  `2dlidar-phase3b-scanaccum-imufix.md`. Purpose of this run: hold the
  odometry fix constant and vary only the map, to isolate whether any
  remaining gap after the odometry fix is attributable to map quality.

## Overall Result: **FAIL**

## Full-Overlap Statistics

| Metric | Value |
|---|---|
| n (pairs) | 810 |
| Trans. mean (m) | 32.3935 |
| Trans. RMS (m) | 54.6019 |
| Trans. max (m) | 158.1110 |
| Trans. p95 (m) | 126.8876 |
| Yaw mean \|err\| (rad) | 1.4588 |
| Yaw max \|err\| (rad) | 3.1308 |

## Motion-Window Statistics

_Motion-window analysis disabled (`--no-motion-window`); this bag moves throughout the recording, so a fixed stationary/motion split is not meaningful here. Only full-overlap statistics are reported._

## Thresholds (applied to full-overlap stats)

| Threshold | Value | Limit | Result |
|---|---|---|---|
| Mean translational error < 1.0 m | 32.3935 | 1.0000 | FAIL |
| p95 translational error < 2.5 m | 126.8876 | 2.5000 | FAIL |
| Mean \|yaw\| error < 0.2 rad | 1.4588 | 0.2000 | FAIL |

## Notes

- Replay rate: 1.0x for both GT and PF recordings, replayed with `--clock` (sim time).
- GT time source: `--gt-time-source=bag` (rosbag2 storage receive-time, used because the GT bag was itself replayed to produce the PF run; see _read_bag() docstring in compare_poses.py for why header.stamp would give zero overlap here).
- PF params: `range_method=cddt`, particle count per vendored `config/localize.yaml` default (4000), `max_range=30.0` (outdoor VLP-32C), `scan_topic=/scan`, `odometry_topic=/odom`.
- Odometry input to PF was synthesized wheel+IMU planar unicycle integration (`scripts/2dlidar/wheel_imu_odom.py`), not a native wheel encoder feed — odometry quality directly affects PF motion-model accuracy between scans.
- GT is Autoware NDT `/localization/kinematic_state`; PF publishes pose in the occupancy-grid frame, which is sliced from the same PCD map as NDT's map frame — poses are directly comparable without a transform.
- Z-band caveat: comparison is 2D (x, y, yaw) only; the GT track's z is Autoware's 3D NDT estimate while PF is inherently 2D, so z is not compared.
- `/scan` was bridged from `/sensing/lidar/velodyne_points` via `pointcloud_to_laserscan` with a z-band filter (see Task 3); QoS was bridged from `pointcloud_to_laserscan`'s best-effort publisher to `particle_filter`'s reliable-QoS subscription.
- **Diagnostic note (not a fix):** the run above FAILs the Phase 3 gate. Full-overlap trans. mean=32.39 m, p95=126.89 m, yaw mean|err|=1.459 rad (thresholds: mean<1.0 m, p95<2.5 m, yaw<0.2 rad). Plausible contributors: (1) the synthesized wheel+IMU odometry feeding PF's motion model has no absolute correction and can accumulate heading error, which a particle filter with too few effective particles or a poor sensor model may fail to correct via scan matching; (2) `range_method=cddt` / particle count / sensor-model noise parameters were carried over from vendored defaults and may not be tuned for this scenario; (3) possible scan-to-map mismatches (z-band filter choice, `max_range`) reducing effective localization likelihood signal; (4) ground-truth quality itself (see report notes on the GT source) may be a contributing factor. PF parameter tuning is explicitly out of scope for this gate and is deferred to a Phase-3 follow-up.

## Interpretation

**Map quality matters, conditional on odometry being sane.** With the
IMU yaw-sign fix held constant, the PCD-sliced grid (this run: mean
32.39 m, p95 126.89 m, yaw 1.459 rad) performs *worse* than the
scan-accumulated grid (`2dlidar-phase3b-scanaccum-imufix.md`: mean
20.16 m, p95 89.81 m, yaw 1.318 rad) — roughly the same relative gap as
before the odometry fix (31.0 m vs 25.25 m pre-fix). It is also
essentially unchanged from its own pre-fix baseline (31.0 m ->
32.39 m, within run-to-run noise, not an improvement), unlike the
scan-accumulated grid which improved substantially with the same fix
(25.25 m -> 20.16 m). Combined picture across all four sample-site runs:

| Map | Odometry | Trans. mean (m) | Trans. p95 (m) | Yaw mean\|err\| (rad) |
|---|---|---|---|---|
| PCD-sliced (scanplane) | raw IMU | 31.01 | 121.74 | 0.979 |
| Scan-accumulated | raw IMU | 25.25 | 126.68 | 1.603 |
| PCD-sliced (scanplane) | IMU sign-fixed | 32.39 | 126.89 | 1.459 |
| Scan-accumulated | IMU sign-fixed | 20.16 | 89.81 | 1.318 |

The scan-accumulated grid is the only one that benefits from the
odometry fix; the PCD-sliced grid does not. That is consistent with the
original scan-accumulation hypothesis (map/scan structural mismatch
contributes to drift) being real but secondary to — and only visible
once corrected for — the dominant odometry bug. Neither combination
clears the Phase 3 gate; best result (scan-accumulated + IMU fix) is
still ~20x over the mean threshold. Next candidates: PF sensor-model/
particle-count tuning (still untested) and whether the raw wheel+IMU
odometry's *velocity* channel (not just yaw) has its own quality issues.
