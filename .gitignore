# editor
*.swp
*.swo
*~
.*~
\#*#

# colcon
/build/
/log/
/install/
/scripts/map/_projector_cpp/build/
/scripts/map/_projector_cpp/install/
/scripts/map/_projector_cpp/log/

# python
__pycache__/
*.pyc
*.pyo

# tmp
tmp/

# play records
play_log/

# downloaded datasets
data/leodrive-busood/
# Generated by `just setup-autoware-data`: symlinks into /opt/autoware/*/data
# plus the .engine files TensorRT writes there.
data/autoware_data/

# `just export-engines` output: a release asset, ~50 MB of .engine files named
# by this board's fingerprint. Uploaded to a GitHub Release, never committed --
# same reasoning as data/autoware_data above, at release scale.
/orin-*.tar.gz
/desktop-*.tar.gz
# export-images.sh writes here by default, and these are whole images.
docker/desktop/*.tar.gz
docker/desktop/*.tar.gz.sha256
docker/desktop/AutoSDV-*.tar.gz

# Typst build artefacts
docs/tech/*.pdf

# Setup state, machine-local: what ran here, and what changed since.
setup/.state.json
setup/.markers/
